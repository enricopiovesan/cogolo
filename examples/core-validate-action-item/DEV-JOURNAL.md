# core.validate-action-item — dev journal

- Source contract: Loop capability package v2 `core.validate-action-item@1.0.0`, adapted for Traverse deserializer and registry persona_ref resolution.
- Guest: `#![no_std]` validator over `validation_config` + `existing_open_items`.
- Smoke: `bash scripts/ci/core_validate_action_item_example_smoke.sh`.
- Ticket: traverse-framework/traverse#1029.
