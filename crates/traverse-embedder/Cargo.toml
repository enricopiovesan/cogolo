[package]
name = "traverse-embedder"
description = "Public Traverse platform embedder SDK for Rust hosts (Linux GTK and CLI clients), implementing embedder-api/1.0.0"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
semver = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.145"
sha2 = "0.10"
traverse-contracts = { workspace = true }
traverse-registry = { workspace = true }
traverse-runtime = { workspace = true }

[dev-dependencies]
wat = "1"

[lints]
workspace = true
